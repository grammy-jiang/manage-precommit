"""manage-precommit: a Claude Code skill that builds a repository's pre-commit config.

The package exists to deliver the skill. Everything that does work lives under
``skill/``; see ``skill/SKILL.md`` for the procedure and ``skill/scripts/`` for
the tools it drives.
"""

__version__ = "0.2.0"
